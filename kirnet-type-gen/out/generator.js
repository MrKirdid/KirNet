"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateTypesContent = generateTypesContent;
exports.writeTypesFile = writeTypesFile;
var fs = __importStar(require("fs"));
var path = __importStar(require("path"));
/**
 * Generate the Types.luau content from parsed service definitions.
 *
 * The output is a **drop-in replacement** for KirNet.  Users swap their
 * require path and keep calling `KirNet.GetService("Name")` as normal —
 * Luau intersection-type overloads on GetService narrow the return type
 * to the correct service type based on the string literal argument.
 */
function generateTypesContent(services, requirePath) {
    var sorted = __spreadArray([], services, true).sort(function (a, b) { return a.name.localeCompare(b.name); });
    var lines = [];
    lines.push("-- AUTO-GENERATED by KirNet Type Generator");
    lines.push("-- Do not edit manually. This file is overwritten on every save.");
    lines.push("--");
    lines.push("-- Require this module instead of KirNet for fully typed GetService().");
    lines.push("-- All other KirNet methods (CreateSignal, RegisterService, etc.) work as normal.");
    lines.push("");
    lines.push("local KirNet = require(".concat(requirePath, ")"));
    lines.push("");
    if (sorted.length === 0) {
        lines.push("return KirNet");
        return lines.join("\n") + "\n";
    }
    // -- Inlined KirNet types so luau-lsp resolves them without require-path resolution
    lines.push("-- ============================================================");
    lines.push("-- KirNet types (inlined for reliable autocomplete)");
    lines.push("-- ============================================================");
    lines.push("");
    lines.push("type Signal<T...> = {");
    lines.push("\tFire: (self: Signal<T...>, player: Player, T...) -> (),");
    lines.push("\tFireAll: (self: Signal<T...>, T...) -> (),");
    lines.push("\tFireExcept: (self: Signal<T...>, player: Player, T...) -> (),");
    lines.push("\tFireList: (self: Signal<T...>, players: { Player }, T...) -> (),");
    lines.push("\tFireServer: (self: Signal<T...>, T...) -> (),");
    lines.push("\tConnect: (self: Signal<T...>, callback: (T...) -> ()) -> (),");
    lines.push("\tOnce: (self: Signal<T...>, callback: (T...) -> ()) -> (),");
    lines.push("\tWait: (self: Signal<T...>, timeout: number?) -> T...,");
    lines.push("\tDisconnect: (self: Signal<T...>) -> (),");
    lines.push("\tOnServerEvent: (self: Signal<T...>, callback: (player: Player, T...) -> ()) -> (),");
    lines.push("}");
    lines.push("");
    lines.push("type Function<TReturn> = {");
    lines.push("\tCall: (self: Function<TReturn>) -> any,");
    lines.push("}");
    lines.push("");
    // -- Service Types section
    lines.push("-- ============================================================");
    lines.push("-- Service Types");
    lines.push("-- ============================================================");
    lines.push("");
    for (var _i = 0, sorted_1 = sorted; _i < sorted_1.length; _i++) {
        var svc = sorted_1[_i];
        var typeName = "".concat(svc.name, "Type");
        lines.push("export type ".concat(typeName, " = {"));
        for (var _a = 0, _b = svc.fields; _a < _b.length; _a++) {
            var field = _b[_a];
            // Strip any leftover KirNet. prefix — generated file defines Signal/Function locally
            var fieldType = field.type
                .replace(/\bKirNet\s*\.\s*Signal\s*</g, "Signal<")
                .replace(/\bKirNet\s*\.\s*Function\s*</g, "Function<");
            lines.push("\t".concat(field.name, ": ").concat(fieldType, ","));
        }
        lines.push("}");
        lines.push("");
    }
    // -- Typed KirNet with GetService overloads
    lines.push("-- ============================================================");
    lines.push("-- Typed KirNet — drop-in replacement with overloaded GetService");
    lines.push("-- ============================================================");
    lines.push("");
    // Build the intersection-type overload for GetService so that
    // KirNet.GetService("CombatService") resolves to CombatServiceType, etc.
    lines.push("type GetServiceOverloads =");
    for (var i = 0; i < sorted.length; i++) {
        var svc = sorted[i];
        var prefix = i === 0 ? "\t" : "\t& ";
        lines.push("".concat(prefix, "((name: \"").concat(svc.name, "\") -> ").concat(svc.name, "Type)"));
    }
    lines.push("\t& ((name: string) -> { [string]: any })");
    lines.push("");
    lines.push("type TypedKirNet = {");
    lines.push("\tGetService: GetServiceOverloads,");
    lines.push("\tCreateSignal: typeof(KirNet.CreateSignal),");
    lines.push("\tCreateFunction: typeof(KirNet.CreateFunction),");
    lines.push("\tRegisterService: typeof(KirNet.RegisterService),");
    lines.push("\tRegisterController: typeof(KirNet.RegisterController),");
    lines.push("\tUseMiddleware: typeof(KirNet.UseMiddleware),");
    lines.push("\tSetDebug: typeof(KirNet.SetDebug),");
    lines.push("}");
    lines.push("");
    lines.push("return (KirNet :: any) :: TypedKirNet");
    return lines.join("\n") + "\n";
}
/**
 * Write the generated Types.luau to disk.
 */
function writeTypesFile(workspaceRoot, outputPath, content) {
    var absPath = path.join(workspaceRoot, outputPath);
    var dir = path.dirname(absPath);
    fs.mkdirSync(dir, { recursive: true });
    fs.writeFileSync(absPath, content, "utf-8");
}
